[package]
name = "nova-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
nova-core = { version = "0.1.0", path = "../nova-core" }
rquickjs = { version = "0.11.0", features = ["classes", "properties"] }
